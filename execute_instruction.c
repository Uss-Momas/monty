#include "monty.h"

/**
 * execute - it executes the right instruction
 * @stack: the pointer to the stack
 * @line_number: the number of the line
 * Return: nothing
 */
void execute_instruction(stack_t **stack, unsigned int line_number)
{
}
